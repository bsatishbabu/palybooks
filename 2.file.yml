---
 - hosts: all
   tasks:
   - name: creating file
     file:
       path: /home/ansible/file786
       state: touch
   - name: creating a directory
     file:
        path: /home/ansible/dir786
        state: directory
